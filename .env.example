# Database Configuration (Optional for Postgres, SQLite used as fallback)
DATABASE_URL=postgresql://user:password@localhost:5432/churniq_db
SQLITE_PATH=data/churniq.db

# AI Provider Credentials (Optional, local analytical engine used as fallback)
GEMINI_API_KEY=your_gemini_api_key_here
OPENAI_API_KEY=your_openai_api_key_here

# App Server Port
PORT=8000
